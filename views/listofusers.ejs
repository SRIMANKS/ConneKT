<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Anek+Latin:wght@200;400;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <h1>list of users in <%= name %></h1>
    <div class="container">
      <% for(var i = 0; i < listofusers.length; i++){%>
      <div class="card">
        <div>
        <p class="heading">USERNAME</p>
        <p class="card-title"><%= listofusers[i].username %></p>
        </div>
        <div>
        <p class="heading">NAME</p>
        <p class="card-text"><%= listofusers[i].name %></p>
        </div>
        <div>
        <p class="heading">ROLLNO</p>
        <p class="card-text"><%= listofusers[i].rollno %></p>
        </div>
        <div>
        <p class="heading">HOSTEL</p>
        <p class="card-text"><%= listofusers[i].hostel %></p>
        </div>
        <div>
        <p class="heading">DEPARTMENT</p>
        <p class="card-text"><%= listofusers[i].dept %></p>
        </div>
        <div>
        <p class="heading">EMAIL</p>
        <p class="card-text"><%= listofusers[i].email %></p>
        <a href="/user/<%= listofusers[i]._id %> "><button id="view">view profile</button></a>
        </div>
      </div>
      <% } %> <% if(listofusers.length==0){%>
      </div>

        <h1 id="nouser">No users in <%= name %></h1>
        <a href="/"><button>home</button></a>
      <% } %>
  </body>
</html>

<style>
  body {
    font-family: "Anek Latin", sans-serif;
    color: white;
    border: 0px;
    margin: 0px;
    width: 100vw;
    display: flex;
    justify-content: space-evenly;
    align-items: center;
    flex-direction: column;
    background-color: #1c1c1c;
  }
  h1{
    font-size: 5em;
    text-align: center;
  }
  .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-gap: 1em;
  }
.card div{
    width:50%;
}
.heading{
    font-size: 1.2em;
    font-weight: bold;
}
  .card {
    padding:1em;
    height: 350px;
    width: 45vw;
    display: flex;
    flex-direction: row;
    justify-content: flex-start;
    flex-wrap: wrap;
    border-radius: 25px;
    background: linear-gradient(
      -105deg,
      rgba(223, 0, 94, 1),
      rgba(254, 90, 90, 1)
    );
    transition: 1s cubic-bezier(0.075, 0.82, 0.165, 1);
  }
  .card:hover {
    transform: scale(1.02);
  }

  #view{
    font-size: 1em;
    background: black;
}
#view:hover{
  color:white;
  background: black;
}

  button {
    padding: 0.5em 1em;
    background: none;
    font-family: "Anek Latin", sans-serif;
    color: aqua;
    margin: 1em 1em;
    font-size: 1.5em;
    transition: 0.5s;
    box-sizing: border-box;
    border: solid 1px #1c1c1c;
  }
  button:hover {
    color:black;
    background-color: aqua;
  }
  @media (max-width: 1200px) {
    .container{
        grid-template-columns: 1fr;
    }
    .card{
        width: 90vw;
    }
  }
  @media (max-width: 700px) {
    body{
        font-size: 0.8em;
    }
  }




</style>
